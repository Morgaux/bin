#!/bin/sh
#
# https://gitlab.com/morgaux/bin
# 

die()
{
err "$@"
exit 1
}

[ -x "$(command -v st)" ] || die "tt: st not installed"

[ -x "$(command -v tabbed)" ] || \
	{ st -t 'suckless terminal' ; die "tt: tabbed not installed" ; }

tabbed -c -r 2 st -w '' -t 'suckless terminal'

