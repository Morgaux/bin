#!/bin/sh

#
# http://gitlab.com/morgaux/bin
#

##
# log a message to ~/var/log
##

[ -d ~/var ] || mkdir -p ~/var

STAMP="$(date +%x' '%X)"
PARENT="$(ps -o comm= $PPID)"
MSG="$@"

echo "$STAMP : $PARENT : $MSG" >> ~/var/log

